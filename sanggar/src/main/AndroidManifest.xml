<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanggar">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".GlobalClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIM_Sanggar">
        <activity android:name=".view.activity.kegiatan.DetailKegiatanActivity"></activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".view.activity.jam_operasional.JamOperasionalActivity" />
        <activity android:name=".view.activity.jadwal_sanggar.JadwalSanggarActivity" />
        <activity android:name=".view.activity.kegiatan.KegiatanActivity" />
        <activity android:name=".view.activity.auth.RegisterActivity" />
        <activity android:name=".view.activity.auth.LoginActivity" />
        <activity android:name=".view.activity.profil.EditProfilActivity" />
        <activity android:name=".MainTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>