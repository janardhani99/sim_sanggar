<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sim_sanggar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIM_Sanggar">
        <activity android:name=".view.activity.auth.SewaActivity"></activity>
        <activity android:name=".view.activity.auth.JadwalLatihanActivity" />
        <activity android:name=".view.activity.auth.RegisterActivity" />
        <activity android:name=".view.activity.auth.LoginActivity" />
        <activity android:name=".MainTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>